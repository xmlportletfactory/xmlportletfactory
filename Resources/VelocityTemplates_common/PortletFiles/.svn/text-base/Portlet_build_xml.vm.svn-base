#set ($project = $commonData.getProjectName())
#set( $createPath = "/" )
#set( $createName = "/build.xml" )
<?xml version="1.0"?>
<!DOCTYPE project>
<project name="${project}-portlet" basedir="." default="deploy">
	<import file="../build-common-portlet.xml" />
</project>
